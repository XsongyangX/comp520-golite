#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <math.h>
void main(){
	{
				char *CODEGEN_BINDING0 = malloc(128); strcpy(CODEGEN_BINDING0, "no");
string CODEGEN_BINDING1x = CODEGEN_BINDING1x;
		int CODEGEN_BINDING2x = 10;
		printf("%d",CODEGEN_BINDING2x); 
		goto BREAK_LABEL3;
		if ( strcmp(CODEGEN_BINDING1x, "yes") == 0){
char CODEGEN_BINDING4x = 'k';
			printf("%d",CODEGEN_BINDING4x); 
		}
		else{
		if ( strcmp(CODEGEN_BINDING1x, "no") == 0){
float CODEGEN_BINDING5x = 3.000000;
			float CODEGEN_BINDING6 = CODEGEN_BINDING5x;
			printf("%s", "no case"); 
		}
		else{
		}
		}
		BREAK_LABEL3:
	}
}
