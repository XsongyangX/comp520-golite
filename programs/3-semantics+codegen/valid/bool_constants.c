#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <math.h>
void main(){
	{
		if ((null)){
char *CODEGEN_BINDING0true = malloc(128);
			strcpy(CODEGEN_BINDING0true, "true or false");
			printf("%s", CODEGEN_BINDING0true); 
			printf("\n");
		}
	}
	if((null)){ printf("True");} else{ printf("False");}
}
